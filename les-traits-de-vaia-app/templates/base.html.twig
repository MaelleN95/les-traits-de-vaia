<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('styles/header-footer.css') }}">
        {% endblock %}


        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body>
    <header>
        <nav class="navbar">
            <div id="navbarMain">
                <p class="logo">Les Traits de Vaïa</p>

                <ul>
                    <li class="nav-item">
                        <a class="nav-link {% if app.request.get('_route') starts with 'shop_' %}active{% endif %}" href="{{ path('shop_index') }}">Boutique</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('cart_index') }}">Mon Panier</a>
                    </li>
                    {% if app.user %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('checkout') }}">Commander</a>
                        </li>
                    {% endif %}
                </ul>
            </div>

            <ul>
                {% if app.user %}
                    <li class="nav-item">
                        <span class="text-muted">Bien le bonjour {{ app.user.fullname }} !</span>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link deconnect" href="{{ path('app_logout') }}">Déconnexion</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_register') }}">Inscription</a>
                    </li>
                {% endif %}
                <li class="nav-item cart-link">
                    <a href="{{ path('cart_index') }}" class="cart-icon nav-link">
                        Panier
                        {% if app.session.get('cart') is not empty %}
                            <div>
                                <span class="cart-count">
                                    {{
                                        app.session.get('cart')|reduce((carry, item) => carry + item, 0)
                                    }}
                                </span>
                            </div>
                        {% else %}
                            <div>
                                <span class="cart-count">0</span>
                             </div>
                        {% endif %}
                    </a>
                </li>
            </ul>
        </nav>
    </header>

    {# ---------- CONTENU PRINCIPAL ---------- #}
    <main class="main-container">
        {% for message in app.flashes('success') %}
            <div class="flash-message flash-success">
                {{ message }}
            </div>
        {% endfor %}

        {% for message in app.flashes('error') %}
            <div class="flash-message flash-error">
                {{ message }}
            </div>
        {% endfor %}

        {% block body %}{% endblock %}
    </main>


    {# ---------- FOOTER ---------- #}
    <footer class="bg-light text-center text-muted py-4 border-top mt-5">
        <div class="container">
            <p class="mb-1">
                <a href="{{ path('shop_index') }}" class="nav-link">Accueil</a> |
                <a href="{{ path('cart_index') }}" class="nav-link">Panier</a> |
                <a href="#" class="nav-link">CGV</a> |
                <a href="#" class="nav-link">Contact</a>
            </p>
            <small>© {{ "now"|date("Y") }} Les Traits de Vaïa — Tous droits réservés</small>
            <p><small>Ce site est une réalisation personnelle à but pédagogique, aucun achat ne peut se réaliser.</small></p>
        </div>
    </footer>
    </body>
</html>
